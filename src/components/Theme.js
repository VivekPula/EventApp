const palettes = 
  {
    accentColor: (opacity) => `rgba(128, 0, 255,${opacity})`,
    secondaryColor: (opacity) => `rgba(238, 222, 255,${opacity})`,
    primaryColor:(opacity)=>`rgba(128,0,255,${opacity})`,
    exColor:(opacity)=>`rgba(136,255,0,${opacity})`,
  }
  ;

export const theme = palettes;
